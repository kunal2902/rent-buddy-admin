"use client";

import { MainComponent, PageHeader } from "@/components";

const LayoutSettingsContainer = () => (
	<MainComponent>
		<PageHeader title="Layout Settings" />
	</MainComponent>
);

export default LayoutSettingsContainer;
