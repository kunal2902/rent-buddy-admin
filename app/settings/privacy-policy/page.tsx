import { MainNavbar, MainSidebar } from '@/components';
import { PrivacyPolicyPageContainer } from '@/containers';

const PrivacyPolicy = () => (
	<>
		<MainNavbar />
		<MainSidebar />
		<PrivacyPolicyPageContainer />
	</>
);

export default PrivacyPolicy;
