import { MainNavbar, MainSidebar } from '@/components';
import { ContactPageContainer } from '@/containers';

const Contact = () => (
	<>
		<MainNavbar />
		<MainSidebar />
		<ContactPageContainer />
	</>
);

export default Contact;
