import { MainNavbar, MainSidebar } from '@/components';
import { TnCPageContainer } from '@/containers';

const TnC = () => (
	<>
		<MainNavbar />
		<MainSidebar />
		<TnCPageContainer />
	</>
);

export default TnC;
