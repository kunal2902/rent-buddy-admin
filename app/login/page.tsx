import { LoginContainer } from "@/components/login-page";

const LoginPage = () => {
	return (
		<div className="w-full min-h-screen flex bg-white flex-col items-center justify-center text-black font-public-sans tracking-wider">
			<LoginContainer />
		</div>
	);
};

export default LoginPage;
