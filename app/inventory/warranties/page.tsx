import { MainNavbar, MainSidebar } from "@/components";
import { WarrantiesContainer } from "@/containers";

const WarrantiesPage = () => (
	<>
		<MainNavbar />
		<MainSidebar />
		<WarrantiesContainer />
	</>
);

export default WarrantiesPage;
